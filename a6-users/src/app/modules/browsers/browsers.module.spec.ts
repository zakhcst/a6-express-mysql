import { BrowsersModule } from './browsers.module';

describe('BrowsersModule', () => {
  const browsersModule = BrowsersModule;

  it('should create an instance', () => {
    expect(browsersModule).toBeTruthy();
  });
});
